#!/bin/bash

# Launching command in gnome terminal
xterm -title "<%= @command %>" -e /bin/bash -c '<%= @command %> && read -p "Press [enter] to close"'

# Return a value based on gnome-terminal execution return
echo '0' > <%= @tmp_file %>
