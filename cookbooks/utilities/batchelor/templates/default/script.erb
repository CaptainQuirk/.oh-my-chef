#!/bin/bash

# Creating an empty file to welcome the return value
# with unique name
touch tmp/<%= @tmp_file %>

# Launching command in gnome terminal
xterm -e /bin/bash -title <%= @command %> -c '<%= @command %> && read -p "Press [enter] to close"'

# Return a value based on gnome-terminal execution return
